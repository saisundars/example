import { MycolordirectiveDirective } from './mycolordirective.directive';

describe('MycolordirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MycolordirectiveDirective()
    expect(directive).toBeTruthy();
  });
});
